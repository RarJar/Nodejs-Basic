<h1>Home Page</h1>

<% blogs.forEach(blog => { %>
    <ul>
        <li><%= blog.title %></li>
        <li><%= blog.description %></li>
        <a href="/blog/<%= blog._id %>">Read</a>
    </ul>
<% }) %>

